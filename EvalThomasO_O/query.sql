CREATE TABLE users ( //Création de la table users
    id INT AUTO_INCREMENT PRIMARY KEY, //Création de la colonne id qui sera clef primaire
    name VARCHAR(100) NOT NULL //Création de la colonne name qui ne peut pas être nulle
);